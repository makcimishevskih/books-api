import { Categories, Order } from 'store/config/types';

export const categoryOptions: Categories[] = ['all', 'art', 'biography', 'computers', 'history', 'poetry', 'medical'];
export const orderByOptions: Order[] = ['newest', 'relevance'];
